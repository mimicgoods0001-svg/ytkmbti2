# 部署问题排查指南

## 当前问题
网站只显示 README.md 的文字内容，而不是实际的应用界面。

## 可能的原因

### 1. GitHub Pages 配置错误
GitHub Pages 可能配置为从源代码分支部署，而不是从 GitHub Actions 构建的产物。

**解决方法：**
1. 进入 GitHub 仓库
2. 点击 **Settings** → **Pages**
3. 在 **Source** 部分，选择 **GitHub Actions**（不是 Branch）
4. 如果显示的是某个分支（如 `main` 或 `gh-pages`），需要改为 **GitHub Actions**

### 2. GitHub Actions 没有运行或失败
检查 GitHub Actions 是否成功运行。

**检查步骤：**
1. 进入 GitHub 仓库
2. 点击 **Actions** 标签
3. 查看最新的工作流运行状态
4. 如果显示 ❌ 失败，点击查看错误日志
5. 如果显示 ✅ 成功，但网站还是不对，可能是配置问题

### 3. 构建配置问题
确保 `next.config.js` 已正确配置 `basePath`。

**已修复的配置：**
```javascript
basePath: '/ytkmbti2',
assetPrefix: '/ytkmbti2',
```

## 解决步骤

### 步骤 1：检查 GitHub Pages 配置
1. 仓库 → **Settings** → **Pages**
2. **Source** 必须选择 **GitHub Actions**
3. 如果显示分支，改为 **GitHub Actions**

### 步骤 2：触发重新部署
1. 确保 `next.config.js` 已更新（包含 basePath）
2. 提交并推送代码到 GitHub
3. 或者手动触发 GitHub Actions：
   - 进入 **Actions** 标签
   - 选择 "Deploy to GitHub Pages" 工作流
   - 点击 **Run workflow**

### 步骤 3：等待部署完成
1. 在 **Actions** 中查看部署进度
2. 等待两个任务完成：
   - ✅ **build** 任务（构建项目）
   - ✅ **deploy** 任务（部署到 GitHub Pages）
3. 通常需要 2-5 分钟

### 步骤 4：验证部署
1. 访问 `https://mimicgoods0001-svg.github.io/ytkmbti2/`
2. 应该看到应用界面，而不是 README 文字
3. 如果还是显示文字，清除浏览器缓存后重试

## 常见错误

### 错误：404 Not Found
- 检查 `basePath` 是否与仓库名匹配
- 仓库名是 `ytkmbti2`，所以 basePath 应该是 `/ytkmbti2`

### 错误：空白页面
- 打开浏览器开发者工具（F12）
- 查看 Console 是否有 JavaScript 错误
- 查看 Network 标签，检查资源是否加载失败

### 错误：样式丢失
- 检查 `assetPrefix` 配置
- 确保 CSS 文件路径正确

## 验证清单

- [ ] GitHub Pages Source 设置为 **GitHub Actions**
- [ ] `next.config.js` 包含正确的 `basePath: '/ytkmbti2'`
- [ ] `next.config.js` 包含正确的 `assetPrefix: '/ytkmbti2'`
- [ ] GitHub Actions 工作流运行成功
- [ ] 构建任务（build）成功完成
- [ ] 部署任务（deploy）成功完成
- [ ] 访问网站看到应用界面（不是 README）
