# 🚀 部署快速参考卡片

## 一、GitHub 部署（5分钟）

### 1. 创建仓库
- 访问 https://github.com
- 点击右上角 **+** → **New repository**
- 仓库名：`ytkMBTI`
- 选择 **Public**
- 点击 **Create repository**

### 2. 上传代码
在 PowerShell 中运行：

```powershell
cd C:\Users\xin\Desktop\ytkMBTI
git add .
git commit -m "首次提交"
git remote add origin https://github.com/你的用户名/ytkMBTI.git
git branch -M main
git push -u origin main
```

**提示**：密码处使用 GitHub Personal Access Token（不是登录密码）

---

## 二、Vercel 部署（3分钟）

### 1. 登录 Vercel
- 访问 https://vercel.com
- 点击 **Continue with GitHub** 登录

### 2. 导入项目
- 点击 **Add New...** → **Project**
- 找到 `ytkMBTI` 仓库，点击 **Import**

### 3. 配置设置
- **Framework Preset**: Next.js（自动）
- **Build Command**: `npm run build`（自动）
- **Output Directory**: 改为 `out` ⚠️ **重要！**
- 点击 **Deploy**

### 4. 完成
- 等待 1-3 分钟
- 复制生成的链接（如 `https://ytkmbti.vercel.app`）
- 分享给朋友！

---

## 🔄 更新代码后

```powershell
git add .
git commit -m "更新说明"
git push
```

Vercel 会自动重新部署！

---

## ⚠️ 重要提示

1. **Output Directory 必须设置为 `out`**
2. **Git 密码使用 Personal Access Token**
3. **每次 push 后 Vercel 自动部署**

---

## 📖 详细步骤

查看 `部署指南-详细步骤.md` 获取完整说明
