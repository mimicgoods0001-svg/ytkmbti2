# 部署版本说明

这是一个精简的部署版本，只包含部署到网页所需的源代码文件。

## 📊 文件统计

- **总大小**: 约 0.35 MB（353 KB）
- **文件数量**: 45 个文件
- **已排除**: `node_modules`、`.next`、`out` 等构建产物

## ✅ 包含的文件

- ✅ 所有源代码文件（`.tsx`、`.ts`、`.css`）
- ✅ 配置文件（`package.json`、`next.config.js`、`tsconfig.json` 等）
- ✅ 静态资源（`public` 目录）
- ✅ GitHub Actions 工作流（`.github/workflows/deploy.yml`）
- ✅ `.gitignore` 文件

## ❌ 已排除的文件

- ❌ `node_modules` - 依赖包（可通过 `npm install` 重新安装）
- ❌ `.next` - 构建缓存（构建时自动生成）
- ❌ `out` - 构建输出（构建时自动生成）
- ❌ `.git` - Git 版本控制历史

## 🚀 部署方式

### 方式一：使用 GitHub Actions 自动部署（推荐）

1. 将整个 `ytkMBTI-deploy` 目录上传到 GitHub
2. 在 GitHub 仓库设置中启用 GitHub Pages
3. 选择 "GitHub Actions" 作为部署源
4. 每次推送代码时，GitHub Actions 会自动：
   - 安装依赖（`npm ci`）
   - 构建项目（`npm run build`）
   - 部署到 GitHub Pages

### 方式二：手动构建后部署

1. 在本地安装依赖：
   ```bash
   npm install
   ```

2. 构建项目：
   ```bash
   npm run build
   ```

3. 将 `out` 目录中的文件上传到静态托管服务（如 Netlify、Vercel 等）

## 📝 注意事项

- 这个版本可以直接上传到 GitHub 或其他代码托管平台
- 部署平台会自动安装依赖并构建项目
- 不需要手动上传 `node_modules` 或 `.next` 文件夹
