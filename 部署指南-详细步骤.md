# ytkMBTI 项目部署指南 - 详细步骤

## 📋 部署流程概览
1. 准备 GitHub 账户和仓库
2. 上传代码到 GitHub
3. 在 Vercel 上部署
4. 获取访问链接并分享

---

## 第一步：准备 GitHub 仓库

### 1.1 登录/注册 GitHub
- 打开浏览器，访问：https://github.com
- 如果没有账户，点击右上角 "Sign up" 注册
- 如果已有账户，点击右上角 "Sign in" 登录

### 1.2 创建新仓库
1. 登录后，点击右上角的 **"+"** 号
2. 选择 **"New repository"**（新建仓库）
3. 填写仓库信息：
   - **Repository name**（仓库名称）：输入 `ytkMBTI`（或你喜欢的名字）
   - **Description**（描述）：可选，例如 "MBTI 性格测试网页"
   - **Visibility**（可见性）：
     - 选择 **Public**（公开）- 这样别人可以看到代码
     - 或选择 **Private**（私有）- 只有你能看到
   - **不要勾选** "Add a README file"（因为本地已有代码）
   - **不要勾选** "Add .gitignore"（本地已有）
   - **不要选择** License
4. 点击绿色的 **"Create repository"** 按钮

### 1.3 复制仓库地址
创建成功后，GitHub 会显示一个页面，上面有仓库地址，类似：
```
https://github.com/你的用户名/ytkMBTI.git
```
**复制这个地址，稍后会用到！**

---

## 第二步：上传代码到 GitHub

### 2.1 检查 Git 是否已安装
1. 按 `Win + R` 打开运行窗口
2. 输入 `powershell` 并回车
3. 在 PowerShell 中输入：
   ```powershell
   git --version
   ```
4. 如果显示版本号（如 `git version 2.x.x`），说明已安装
5. 如果没有安装，访问 https://git-scm.com/download/win 下载安装

### 2.2 初始化 Git 仓库（如果还没有）
1. 在 PowerShell 中，切换到项目目录：
   ```powershell
   cd C:\Users\xin\Desktop\ytkMBTI
   ```
2. 检查是否已经是 Git 仓库：
   ```powershell
   git status
   ```
3. 如果不是 Git 仓库，执行：
   ```powershell
   git init
   ```

### 2.3 配置 Git 用户信息（首次使用需要）
```powershell
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```
（使用你的 GitHub 用户名和邮箱）

### 2.4 添加所有文件到 Git
```powershell
git add .
```
这个命令会把项目所有文件添加到暂存区

### 2.5 提交代码
```powershell
git commit -m "首次提交：MBTI 测试项目"
```

### 2.6 连接到 GitHub 仓库
```powershell
git remote add origin https://github.com/你的用户名/ytkMBTI.git
```
（把 `你的用户名` 替换成你的 GitHub 用户名）

### 2.7 推送到 GitHub
```powershell
git branch -M main
git push -u origin main
```
- 如果提示输入用户名和密码：
  - 用户名：你的 GitHub 用户名
  - 密码：需要使用 **Personal Access Token**（不是登录密码）
  - 如果还没有 Token，看下面的步骤

### 2.8 创建 Personal Access Token（如果需要）
1. 登录 GitHub
2. 点击右上角头像 → **Settings**（设置）
3. 左侧菜单找到 **Developer settings**
4. 点击 **Personal access tokens** → **Tokens (classic)**
5. 点击 **Generate new token** → **Generate new token (classic)**
6. 填写信息：
   - **Note**：输入 "Vercel部署" 或任意描述
   - **Expiration**：选择过期时间（建议 90 天或 No expiration）
   - **Select scopes**：勾选 **repo**（全部权限）
7. 点击 **Generate token**
8. **重要**：复制生成的 token（只显示一次，要保存好！）
9. 推送代码时，密码处粘贴这个 token

---

## 第三步：在 Vercel 上部署

### 3.1 注册/登录 Vercel
1. 打开浏览器，访问：https://vercel.com
2. 点击右上角 **"Sign Up"** 或 **"Log In"**
3. 选择 **"Continue with GitHub"**（使用 GitHub 账户登录）
4. 授权 Vercel 访问你的 GitHub 账户

### 3.2 导入项目
1. 登录 Vercel 后，点击 **"Add New..."** → **"Project"**
2. 在项目列表中，找到你的 `ytkMBTI` 仓库
3. 点击 **"Import"** 按钮

### 3.3 配置项目设置
Vercel 会自动检测到这是 Next.js 项目，通常不需要修改：
- **Framework Preset**：应该是 "Next.js"（自动检测）
- **Root Directory**：保持默认（`./`）
- **Build Command**：`npm run build`（自动填充）
- **Output Directory**：`.next`（自动填充）
- **Install Command**：`npm install`（自动填充）

**重要**：由于项目使用了 `output: 'export'`，需要修改配置：
1. 点击 **"Build and Output Settings"** 展开
2. 找到 **"Output Directory"**
3. 改为：`out`（因为静态导出会生成 `out` 文件夹）

### 3.4 环境变量（如果有）
如果你的项目需要环境变量（如 API 密钥），在这里添加。这个项目看起来不需要，可以跳过。

### 3.5 部署
1. 点击页面底部的 **"Deploy"** 按钮
2. 等待部署完成（通常 1-3 分钟）
3. 部署过程中会显示构建日志

### 3.6 获取访问链接
部署成功后：
1. 你会看到一个 **"Congratulations!"** 页面
2. 页面上会显示你的网站链接，类似：
   ```
   https://ytkmbti.vercel.app
   或
   https://ytkmbti-你的用户名.vercel.app
   ```
3. **点击这个链接**，你的网站就可以访问了！

---

## 第四步：自定义域名（可选）

### 4.1 添加自定义域名
1. 在 Vercel 项目页面，点击 **"Settings"** 标签
2. 左侧菜单找到 **"Domains"**
3. 输入你的域名（如 `mbti.yourdomain.com`）
4. 按照提示配置 DNS 记录

### 4.2 使用 Vercel 提供的域名
Vercel 会自动提供一个免费域名，格式：
- `项目名.vercel.app`
- 或 `项目名-用户名.vercel.app`

---

## 第五步：更新代码后重新部署

### 5.1 本地修改代码后
```powershell
cd C:\Users\xin\Desktop\ytkMBTI
git add .
git commit -m "更新：描述你的修改"
git push
```

### 5.2 Vercel 自动部署
- Vercel 会自动检测到 GitHub 的更新
- 自动触发新的部署
- 通常 1-2 分钟后，新版本就会上线

---

## 常见问题解决

### ❌ 问题1：Git push 失败，提示认证错误
**解决**：使用 Personal Access Token 而不是密码

### ❌ 问题2：Vercel 部署失败
**检查**：
1. 确保 `package.json` 中的 `build` 脚本正确
2. 检查构建日志中的错误信息
3. 确保所有依赖都已安装

### ❌ 问题3：网站显示 404 错误
**解决**：
1. 检查 Vercel 的 Output Directory 是否设置为 `out`
2. 确保 `next.config.js` 中有 `output: 'export'`

### ❌ 问题4：图片不显示
**解决**：
1. 确保图片在 `public` 文件夹中
2. 检查 `next.config.js` 中 `images.unoptimized: true` 已设置

---

## 📝 快速命令参考

```powershell
# 进入项目目录
cd C:\Users\xin\Desktop\ytkMBTI

# 检查状态
git status

# 添加所有更改
git add .

# 提交
git commit -m "更新说明"

# 推送到 GitHub
git push

# 查看远程仓库
git remote -v
```

---

## ✅ 部署检查清单

- [ ] GitHub 账户已创建
- [ ] GitHub 仓库已创建
- [ ] 代码已推送到 GitHub
- [ ] Vercel 账户已注册（使用 GitHub 登录）
- [ ] 项目已导入到 Vercel
- [ ] Output Directory 设置为 `out`
- [ ] 部署成功
- [ ] 网站可以正常访问
- [ ] 测试了所有功能

---

## 🎉 完成！

部署完成后，你就可以：
1. 分享链接给朋友
2. 在社交媒体上推广
3. 通过链接让任何人访问你的 MBTI 测试网站

**记住**：每次修改代码后，只需要 `git push`，Vercel 会自动重新部署！
